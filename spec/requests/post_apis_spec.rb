require 'rails_helper'

RSpec.describe "PostApis", type: :request do
  describe "POST /api/v1/post" do
    # it "creates the post saves the images in local directory and sends email notification to all the other user" do
    #   post '/api/v1/post'
    #   expect(response).to have_http_status(200)
    # end
  end
end
